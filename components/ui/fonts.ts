import { Roboto_Flex } from 'next/font/google';

export const robotoBold = Roboto_Flex({
  subsets: ['latin'],
  weight: ['700'],
  display: 'fallback',
});

export const robotoBlack = Roboto_Flex({
  subsets: ['latin'],
  weight: ['900'],
  display: 'fallback',
});
